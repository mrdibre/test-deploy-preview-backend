# Server Configuration
PORT=3000
HOST=0.0.0.0

# CORS Configuration
# Set to specific origin(s) for production (e.g., https://yourdomain.com)
# Use 'true' to allow all origins (development only)
CORS_ORIGIN=true