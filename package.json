{
  "name": "backend",
  "version": "1.0.0",
  "description": "Simple Node.js Fastify app with hello world endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "vercel-deploy": "tsx vercel-deploy.ts",
    "vercel-deploy:build": "tsc vercel-deploy.ts --outDir dist && node dist/vercel-deploy.js"
  },
  "packageManager": "pnpm@9.4.0",
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "dotenv": "^16.4.5",
    "@vercel/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
